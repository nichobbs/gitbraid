# Remediation Audit — 2026-04-24

Audit performed after rebasing `claude/code-review-report-J9MhV` onto
`origin/main` (103 commits since the fork point).

The rebase was a clean fast-forward: both of the review-branch commits
(`e5ddb20`, `853eb6b`) were merged back into `main` via PRs #2 and #9
and are now part of history. The review and the remediation-plan files
live at `docs/reviews/00-10-*.md` and `docs/remediation/outstanding.md`
respectively on `main`; the `docs/reviews/11-16-*.md` tier files from
the earlier plan were superseded by the numbered tasks T1–T86 captured
under the now-consolidated `docs/remediation/outstanding.md` list.

This document cross-references every ID from the earlier plan
(`12-remediation-p0.md` … `16-remediation-quick-wins.md`) against the
current codebase and marks each as **Done**, **Partial**, or **Open**.

## P0 — hot-fix blockers (target 0.1.1) — **all done**

| ID | Title | Status | Evidence |
| --- | --- | --- | --- |
| SEC-001 | `exec`→`spawn` migration | **Done** | `rg "util.promisify.*exec"` → 0; `gitFunctions.ts:3,365` and `hunkRouter.ts:1,282` only use `spawn` (ref-format + apply); `gitRunner.ts` is the sole runner. |
| SEC-002 | Branch-name validation via `check-ref-format` | **Done** | `gitFunctions.ts:365` spawns `git check-ref-format --branch <name>`. |
| SEC-003 | Path containment guard | **Done** | `src/pathGuard.ts` (`requireInside`); used by `diffEngine.ts:4`. |
| SEC-004 | Credential redaction in logs | **Done** | `gitFunctions.ts:25-64` — `redactCredentials` wraps every `exec`/`stderr` log. |
| BUG-001 | `discardChanges` via `git restore` not `git clean` | **Done** | Legacy `commands.ts` removed; `branchScmProvider.ts:540,576` use `git restore --staged --worktree`. |
| BUG-002 | `revList` / `branch()` stdout bug | **Done** | `gitFunctions.ts:121` returns typed `{ ahead, behind }` via `_run(['rev-list', ...])`. |
| BUG-003 | `activationEvents` malformed | **Done** | `package.json:49` → `workspaceContains:.git` + `workspaceContains:.worktrees/local-config.json`. Soft-exit handled in `extension.ts`. |
| BUG-004 | Hunk-index drift | **Done** | `configTypes.ts` schema v2 with `bodyHash` anchors; `HunkRouter._reconcileAssignments` emits a warning on fuzzy relocation (per `outstanding.md` T8). |
| PKG-001 | Publisher unification | **Done** | `package.json:7` → `nichobbs`. `rg "nihobbs\|kherring"` returns only historical citations in review docs. |
| PKG-002 | `languageModelTools` manifest | **Done** | `package.json:755` declares 13 tools (7 original + 6 additions including `gitbraid_getStackDiagram`, `gitbraid_assignGlob`). |
| PKG-003 | `capabilities` + engine bump | **Done** | `package.json:40` → `^1.95.0`; `capabilities` declares `virtualWorkspaces:false` + `untrustedWorkspaces:"limited"`; `extensionKind:["workspace"]`. |
| PKG-004 | `private: false` | **Done** | `package.json:8`. |

**Result:** every P0 item is complete. Hot-fix gate is passed.

## P1 — feature / architecture consolidation (target 0.2.0) — **done or exceeded**

| ID | Title | Status | Evidence |
| --- | --- | --- | --- |
| ARCH-001 | Retire legacy worktree view | **Done** | `worktreeView.ts` / `worktreeNodes.ts` removed; only `worktreeHealthService.ts` remains (a different service). |
| ARCH-002 | Single `FileChangeBus` | **Done** | `src/fileChangeBus.ts` present; honours `files.watcherExclude` (`fileChangeBus.ts:62-98`). |
| ARCH-003 | Wire `StackResolver` to a `TextDocumentContentProvider` | **Done** | `src/stackContentProvider.ts` registered in `extension.ts:163-169`. |
| ARCH-004 | Bidirectional sync | **Done** | `workspaceSync.ts:140,473,524` — flag-gated, reverse-copy implemented; covered by `test/workspaceSync.bidir.test.ts`. |
| ARCH-005 | Drop dynamic `await import` | **Done** | `rg "await import" src/` returns no hits. |
| FEAT-001 | Drag-and-drop reassignment | **Done** | `branchStackTreeProvider.ts:234-420` — `TreeDragAndDropController`, internal reorder + `text/uri-list` external drops. (`outstanding.md` T17 entry is stale.) |
| FEAT-002 | Stack-wide summary view | **Done** | Stack diagram surfaced via `gitbraid_getStackDiagram` LM tool + clipboard command (commits `d0b107a`, `8375bdd`). |
| FEAT-003 | Stable hunk identifiers | **Done** | Schema `v2` with `bodyHash` anchors; migration path in `configTypes.migrateConfig`. |
| FEAT-004 | Push / sync / rebase stack commands | **Done** | `src/stackCommands.ts:68 pushStack`, `:143 syncStack`; `rebaseRecovery.ts` handles rebase flow. Commit `9772e8f` (T67). |
| FEAT-005 | Expanded exported API | **Done** | `GitBraidApiFacade` now delegates `pullBranch`, `syncBranch`; `onDidSyncFile`/`onDidFloatFile` exposed. |
| FEAT-006 | Symmetrical LM tool set | **Done** | 13 tools declared — `gitbraid_` naming, glob/checkpoint/share additions; `mbc_` prefix fully retired. |
| FEAT-007 | Dynamic default branch detection | **Done** | `gitFunctions.ts:79` `defaultBranch()` + `git symbolic-ref` fallback. |
| UX-001 | Finish branding rollover | **Done** | `package.nls.json` localises titles; all in-product strings read "GitBraid"; `mbc_` prefix removed (commit `64f74f0`). |
| UX-002 | Activity-bar container | **Done** | `package.json:116-121` `viewsContainers.activitybar.gitbraid`. |
| UX-003 | Walkthrough assets | **Done** | `resources/walkthrough-{add-branch,assign-file,commit,rebase}.svg` all present. |
| UX-004 | Settings are honoured | **Done** | `getConfiguration('gitbraid')` read in `workspaceSync.ts:18,140,421`, `branchScmProvider.ts:730`, `fileDecorationProvider.ts:135`, `prAwareness.ts:113`, `rebaseSuggestionService.ts:35`. |

**Result:** every P1 item is complete. Several items in this tier were
exceeded: `checkpointService.ts`, `stackShareService.ts`,
`undoStack.ts`, and `rebaseRecovery.ts` deliver P3 roadmap items
early.

## P2 — quality / tests / performance — **mostly done**

| ID | Title | Status | Evidence / Gap |
| --- | --- | --- | --- |
| TEST-001 | Test-harness isolation | **Done** | `tmp-repo` helper landed (commit `94bff15` P2 checkpoint E). |
| TEST-002 | Missing unit tests | **Partial** | New tests for `branchScmProvider`, `hunkCodeLens`, `lmTools`, activation landed. Coverage threshold still below target (see TEST-004). |
| TEST-003 | Shell-injection regression suite | **Done** | `test/injection.test.ts` — 40 parametric tests (outstanding.md T58). |
| TEST-004 | CI pipeline + coverage floor | **Partial** | `.github/workflows/ci.yml` exists with a 55%/45% floor. **Original target was 70%/60%**. This is the single largest still-open item. |
| ERR-001 | Logger fixes | **Done** | `channelLogger.ts` cached singleton, no double-fire, guarded stack-trace helper. |
| ERR-002 | Typed errors at API boundary | **Done** | `errorSurfacer.ts` + `GitError`, `RebaseConflictError`, `WorktreeExistsError` subtypes. Unused error types removed. |
| ERR-003 | Stderr only on non-zero exit | **Done** | `gitFunctions.ts:56-64` — stderr only surfaces on non-zero exit; CRLF warnings no longer pop. |
| ERR-004 | `Promise.allSettled` for per-branch refresh | **Done** | `branchScmProvider._refreshAll`, `rebaseSuggestionService._checkAll` switched. |
| PERF-001 | Honour `syncDebounceMs` | **Done** | Read via `workspaceSync.ts:18`. |
| PERF-002 | CodeLens debounce | **Done** | Per-URI 300ms debouncer in `hunkCodeLensProvider.ts`; cache keyed by `document.version`. |
| PERF-003 | Parallelise startup worktree creation | **Done** | Bounded parallel in `BranchStackService.initStack` with concurrency cap. |
| PERF-004 | Per-branch SCM status cache | **Done** | P2 checkpoint B (`721e373`) — T45/T47/T53 caches. |
| PERF-005 | Bound in-memory caches | **Done** | `HunkCodeLensProvider._cache` LRU; `_floatingDirty` bounded by `FLOATING_DIRTY_CAP` (see finding below). |
| QA-001 | Lint rules as guard rails | **Done** | `.eslintrc.json` enforces no-restricted-imports on `child_process`, `no-floating-promises`, `no-console`. |

### Still open on P2

- **TEST-004 / CI coverage threshold**: floor is 55%/45% on `main`;
  original T61 goal 70%/60%. `workspaceSync.ts` (555 lines),
  `gitFunctions.ts` (354 lines), and the new `src/commands/`
  modules have the most uncovered lines. Follow-up ticket exists in
  `outstanding.md`.

## P3 — roadmap — **partially delivered early**

| ID | Title | Status | Evidence |
| --- | --- | --- | --- |
| RM-001 | Conflict-recovery UI | **Done** | `src/rebaseRecovery.ts`. |
| RM-002 | Batch "assign subtree to branch" | **Done** | `assignGlob` command + LM tool (commit `283a5fd`). |
| RM-003 | Undo stack | **Done** | `src/undoStack.ts`. |
| RM-004 | PR awareness | **Done** | `src/prAwareness.ts` honours `prDecorationsEnabled`. |
| RM-005 | Multi-root workspace | **Done** | `src/folderRegistry.ts`, `src/folderContext.ts` (commit `ec261a1`). |
| RM-006 | Virtual workspace support | **Won't fix** | Declared `virtualWorkspaces: false`; document in CHANGELOG. |
| RM-007 | MCP server | **Deferred** | ADR landed: `docs/adr/0001-mcp-server.md`. |
| RM-008 | Exportable stack templates | **Done** | `src/stackShareService.ts` + `assignGlob` templates. |
| RM-009 | Keybindings + palette | **Partial** | Command palette entries exist; default keybindings still not declared in `package.json`. |
| RM-010 | Telemetry | **Deferred** | Not in scope until 1.0. |
| RM-011 | Config hot-reload | **Done** | Commit `8f18526` — `add config reload functionality and file watcher for external edits`. |
| RM-012 | Stacked-PR tool interop | **Open** | Not started. |

## Quick wins — all trivially verifiable — **done**

Spot-checked:

- QW-002 `"Multie"` typo: resolved (no hits in `package.json` / `package.nls.json`).
- QW-003 `isPrumary` typo: obsoleted by ARCH-001 (legacy view removed).
- QW-004 `patchToWorktree`: command deleted.
- QW-008 Sonar deps: not in `package.json`.
- QW-009 TS loaders: single loader retained.
- QW-011 Single `.gitignore` writer: `extension.ts` 298 lines — legacy duplicate helper removed.
- QW-014 `log.info('197 ')` artefacts: gone.
- QW-015 `getWorkspaceRoot()` helper: replaced by `folderContext` per-root plumbing.
- QW-022 Build/compile alias: consolidated.
- QW-023 `gitbraid.getStarted`: walkthrough reachable via Command Palette.

## Residual risk register

1. **Coverage floor**: raise from 55/45 → 70/60 incrementally as new
   tests land. Prioritise `workspaceSync.ts`, `gitFunctions.ts`, the
   new `src/commands/` split.
2. **`files.watcherExclude` glob coverage**: finding #4 from the
   2026-04-22 follow-up is addressed in `fileChangeBus.ts:96-103`, but
   the minimal glob matcher only handles the common patterns
   (`**/*`, leading `**/`, trailing `/**`, literal path). Complex user
   globs may still leak through. Audit against `micromatch` behaviour
   before a v1.0 cut.
3. **Stacked-PR tool interop** (RM-012): no automated import path from
   Graphite / git-spr / git-stack. Low priority; open as a standalone
   RFC when there is real user demand.
4. **Default keybindings** (RM-009): still absent from `package.json`.
   Tiny fix; add for v0.2.x.
5. **`outstanding.md` is slightly stale** — the T17 DnD entry is
   already resolved by `branchStackTreeProvider.ts:234-420`. Remove
   from `outstanding.md`.

## Recommended next steps

1. Strike T17 from `docs/remediation/outstanding.md` (stale).
2. Open follow-up tickets for:
   - Coverage: 55%→60% immediate; 60%→65% next sprint.
   - RM-009: default keybindings for `gitbraid.addStackBranch`,
     `gitbraid.assignFile`, `gitbraid.routeHunks`.
   - `fileChangeBus._matchGlob`: either call out the supported syntax
     in the JSDoc or adopt `micromatch`.
3. Tag 0.1.1 / 0.2.0 releases per the plan's release-gate criteria.

## Conclusion

The remediation covered the entire review spec. Every P0 (hot-fix
blocker) and P1 (feature / architecture) item is closed. P2 is
substantially complete with one remaining quality gap (coverage
threshold). P3 landed ahead of schedule on most items (undo stack,
rebase recovery, multi-root, PR awareness, checkpoints, templates,
hot-reload), with MCP deferred behind an ADR and virtual-workspace /
telemetry marked out-of-scope by design.
